{{- if .Values.ingress.enabled -}}
{{- $fullName := include "opentakserver.fullname" . -}}
{{- $svcBackendPort := .Values.backend.service.port -}}
{{- $svcFrontendPort := .Values.frontend.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "opentakserver.labels" . | nindent 4 }}

  annotations:
    traefik.ingress.kubernetes.io/router.tls.options: {{ .Release.Namespace }}-{{ include "opentakserver.fullname" . }}-ots-mtls@kubernetescrd
    {{- $middlewares := list (printf "%s-%s-ots-pass-cert@kubernetescrd" .Release.Namespace (include "opentakserver.fullname" .)) }}
    {{- if .Values.mediamtx.enabled }}
    {{- $middlewares = append $middlewares (printf "%s-%s-strip-webrtc@kubernetescrd" .Release.Namespace (include "opentakserver.fullname" .)) }}
    {{- end }}
    traefik.ingress.kubernetes.io/router.middlewares: {{ join "," $middlewares }}
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ if .serviceName }}{{ .serviceName }}{{ else }}{{ if eq .backend "backend" }}{{ $fullName }}-backend{{ else }}{{ $fullName }}-frontend{{ end }}{{ end }}
                port:
                  number: {{ if .servicePort }}{{ .servicePort }}{{ else }}{{ if eq .backend "backend" }}{{ $svcBackendPort }}{{ else }}{{ $svcFrontendPort }}{{ end }}{{ end }}
          {{- end }}
    {{- end }}
{{- end }}
